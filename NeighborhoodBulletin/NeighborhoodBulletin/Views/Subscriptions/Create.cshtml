@model Domain.Subscription

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Subscription</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @Html.HiddenFor(model => model.NeighborId)
            @*<div class="form-group">
                <label asp-for="NeighborId" class="control-label"></label>
                <select asp-for="NeighborId" class ="form-control" asp-items="ViewBag.NeighborId"></select>
            </div>*@

            @Html.HiddenFor(model => model.ShopOwnerId)
            @*<div class="form-group">
                <label asp-for="ShopOwnerId" class="control-label"></label>
                <select asp-for="ShopOwnerId" class ="form-control" asp-items="ViewBag.ShopOwnerId"></select>
            </div>*@
            @Html.HiddenFor(model => model.SubscriptionStatus)
            @*<div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="SubscriptionStatus" /> @Html.DisplayNameFor(model => model.SubscriptionStatus)
                    </label>
                </div>
            </div>*@
            <div class="form-group">
                Confirm subscription creation.<input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
